# =============================================================================
# UGLI Design - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values
# NEVER commit .env to version control
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED - Application will not start without these
# -----------------------------------------------------------------------------

# PostgreSQL database connection string (Neon serverless recommended)
DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require

# Session secret - use a long random string (min 32 characters)
# Generate with: openssl rand -base64 32
SESSION_SECRET=your-secure-session-secret-here

# -----------------------------------------------------------------------------
# AUTHENTICATION - Required for user login
# -----------------------------------------------------------------------------

# Google OAuth (for Google Sign-In)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Replit Auth (if deployed on Replit)
# These are auto-populated by Replit
# REPL_ID=your-repl-id
# ISSUER_URL=https://replit.com/oidc
# REPLIT_DOMAINS=your-domain.repl.co

# -----------------------------------------------------------------------------
# AI/IMAGE GENERATION - Required for core features
# -----------------------------------------------------------------------------

# Google Gemini API Key (for image generation)
# Get from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key

# Multiple Gemini keys for load balancing (comma-separated)
# GEMINI_API_KEYS=key1,key2,key3

# Alternative Gemini key variable (for Replit AI integrations)
# AI_INTEGRATIONS_GEMINI_API_KEY=your-key

# Replicate API Token (for background removal)
# Get from: https://replicate.com/account/api-tokens
REPLICATE_API_TOKEN=your-replicate-api-token

# -----------------------------------------------------------------------------
# PAYMENTS - Required for billing features
# -----------------------------------------------------------------------------

# Stripe API Keys
# Get from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_live_your-stripe-secret-key
STRIPE_PUBLISHABLE_KEY=pk_live_your-stripe-publishable-key

# Stripe Webhook Secret (for payment notifications)
# Get from: https://dashboard.stripe.com/webhooks
STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret

# -----------------------------------------------------------------------------
# MONITORING & LOGGING - Optional but recommended for production
# -----------------------------------------------------------------------------

# Sentry DSN for error tracking
# Get from: https://sentry.io/settings/projects/
SENTRY_DSN=https://your-key@sentry.io/project-id

# Enable Sentry in non-production environments
# SENTRY_ENABLED=true

# Log level: debug, info, warn, error (default: info in production, debug in dev)
LOG_LEVEL=info

# Log format: json or text (default: text)
LOG_FORMAT=json

# -----------------------------------------------------------------------------
# CORS & SECURITY - Optional
# -----------------------------------------------------------------------------

# Additional allowed origins for CORS (comma-separated)
# ALLOWED_ORIGINS=https://your-domain.com,https://app.your-domain.com

# -----------------------------------------------------------------------------
# DEVELOPMENT ONLY - Never use in production
# -----------------------------------------------------------------------------

# Test mode bypasses authentication (DANGEROUS - never enable in production)
# TEST_MODE=false

# Server port (default: 5000)
PORT=5000

# Node environment
NODE_ENV=development
